import VideoCall from '@/components/videoCall'

const Page = ({ }: { home: React.ReactNode; login: React.ReactNode }) => {
    return (
        <div>
            <VideoCall />
        </div>
    );
};

export default Page;
