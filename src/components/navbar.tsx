import { FunctionComponent } from "react";

interface Props {}

const Navbar: FunctionComponent<Props> = () => {
    return <nav className="bg-beige p-8 text-right">Demo User</nav>;
};

export default Navbar;
